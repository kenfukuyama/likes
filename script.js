likeCount = 0;

function addLike(element) {
    // console.log(element);
    // likeCount++;
    // element.innerText = likeCount + " like(s)";
    // element++;
    element.innerText++;
}


function like(element) {
    element.innerText++;
}



